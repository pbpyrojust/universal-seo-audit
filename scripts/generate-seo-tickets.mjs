#!/usr/bin/env node
import fs from "node:fs";
import path from "node:path";
function parseArgs(argv) { const args={}; for(let i=2;i<argv.length;i++){const a=argv[i]; if(!a.startsWith("--")) continue; const key=a.slice(2); const next=argv[i+1]; if(!next||next.startsWith("--")) args[key]=true; else { args[key]=next; i++; }} return args; }
function parseCsv(csvText){const rows=[]; let row=[], field="", inQuotes=false; const pushField=()=>{row.push(field); field=""}; const pushRow=()=>{ if(!(row.length===1&&row[0]==="")) rows.push(row); row=[];}; for(let i=0;i<csvText.length;i++){ const ch=csvText[i]; if(inQuotes){ if(ch==='"'){ if(csvText[i+1]==='"'){ field+='"'; i++; } else inQuotes=false;} else field+=ch; continue;} if(ch==='"'){ inQuotes=true; continue;} if(ch===','){ pushField(); continue;} if(ch==='\n'){ pushField(); pushRow(); continue;} if(ch==='\r'){ if(csvText[i+1]==='\n') i++; pushField(); pushRow(); continue;} field+=ch;} pushField(); pushRow(); if(!rows.length) return []; const headers=rows[0]; return rows.slice(1).map((r)=>Object.fromEntries(headers.map((h,idx)=>[h,r[idx]??""]))); }
function esc(s){s=String(s??""); return /[",\n\r]/.test(s) ? '"'+s.replace(/"/g,'""')+'"' : s; }
function writeCsv(rows, columns, outPath){ const body=[columns.join(","), ...rows.map((r)=>columns.map((c)=>esc(r[c])).join(","))].join("\n")+"\n"; fs.writeFileSync(outPath, body, "utf8"); }
const args=parseArgs(process.argv); const runDir=args["run-dir"]; if(!runDir){ console.error("ERROR: Missing --run-dir"); process.exit(1);} const issues=parseCsv(fs.readFileSync(path.join(runDir,"seo-issues.csv"),"utf8"));
const globalTypes=new Set(["missing_title","duplicate_title","title_too_long","title_too_short","missing_meta_description","meta_description_too_long","meta_description_too_short","canonical_missing","canonical_multiple","h1_missing","multiple_h1"]);
const globals=new Map(), pages=new Map();
for(const issue of issues){ if(globalTypes.has(issue.issue_type)){ const key=`${issue.issue_type}__${issue.priority}`; const item=globals.get(key)||{ticket_type:"Global",issue_type:issue.issue_type,priority:issue.priority,importance:issue.importance,impact:issue.impact,pages:new Set(),occurrences:0}; item.pages.add(issue.page_url); item.occurrences+=1; globals.set(key,item);} else { const key=`${issue.page_url}__${issue.issue_type}__${issue.priority}`; const item=pages.get(key)||{ticket_type:"Page",issue_type:issue.issue_type,priority:issue.priority,importance:issue.importance,impact:issue.impact,page_url:issue.page_url,occurrences:0,details:issue.details,recommendation:issue.recommendation}; item.occurrences+=1; pages.set(key,item);} }
const rows=[]; for(const item of globals.values()){ const examplePages=[...item.pages].slice(0,5).join(" | "); rows.push({ticket_type:"Global",issue_type:item.issue_type,priority:item.priority,importance:item.importance,impact:item.impact,page_url:"",pages_affected:item.pages.size,occurrences:item.occurrences,example_pages:examplePages,ticket_title:`[SEO][${item.priority}] Fix ${item.issue_type} across site`,ticket_description:`Type: Global\nIssue: ${item.issue_type}\nPriority: ${item.priority}\nImportance: ${item.importance}\nPages affected: ${item.pages.size}\nOccurrences: ${item.occurrences}\nExample pages: ${examplePages}\n\nRecommended action:\nFix this issue in shared templates/components where possible, then re-run the audit.`,ticket_labels:`seo, technical-seo, priority:${String(item.priority).toLowerCase()}, global`}); }
for(const item of pages.values()){ rows.push({ticket_type:"Page",issue_type:item.issue_type,priority:item.priority,importance:item.importance,impact:item.impact,page_url:item.page_url,pages_affected:1,occurrences:item.occurrences,example_pages:item.page_url,ticket_title:`[SEO][${item.priority}] Fix ${item.issue_type} on page`,ticket_description:`Type: Page\nIssue: ${item.issue_type}\nPriority: ${item.priority}\nImportance: ${item.importance}\nPage: ${item.page_url}\nOccurrences: ${item.occurrences}\nDetails: ${item.details}\n\nRecommended action:\n${item.recommendation}`,ticket_labels:`seo, technical-seo, priority:${String(item.priority).toLowerCase()}, page`}); }
const rank={"P0-Critical":0,"P1-High":1,"P2-Medium":2,"P3-Low":3}; rows.sort((a,b)=>{ if(a.ticket_type!==b.ticket_type) return a.ticket_type==="Global"?-1:1; return (rank[a.priority]??99)-(rank[b.priority]??99); }); writeCsv(rows,["ticket_type","issue_type","priority","importance","impact","page_url","pages_affected","occurrences","example_pages","ticket_title","ticket_description","ticket_labels"],path.join(runDir,"seo-ticket-backlog.csv")); console.log(`Wrote: ${path.join(runDir,"seo-ticket-backlog.csv")}`);
